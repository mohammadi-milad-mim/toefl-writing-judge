[project]
name = "toefl-writing-assessor"
version = "0.1.0"
description = "Run three independent LLM assessments for TOEFL writing tasks."
requires-python = ">=3.11"
dependencies = [
    "openai>=2.0.0",
    "python-dotenv>=1.0.1",
    "rich>=13.9.0",
]

[tool.uv]
package = false
